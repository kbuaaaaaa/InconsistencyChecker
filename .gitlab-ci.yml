image: node

stages:
 - build
 - test

build-job:
  tags:
    - docker
  stage: build
  script:
    - echo "Hello world!"

test-job-example:
  tags:
    - docker
  stage: test
  script:
    - npm install unit.js
    - npm install -g mocha
    - npm install jsdom
    - npm install jquery
    - npm install --save-dev nyc
    - npx nyc mocha

