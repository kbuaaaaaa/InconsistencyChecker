image: alpine:latest
build-job:
  tags:
    - docker
  stage: build
  script:
    - echo "Hello, $GITLAB_USER_LOGIN!"

test-job-1:
  tags:
    - docker
  stage: test
  script:
    - echo "This job tests something"
