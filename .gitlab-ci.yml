image: node

stages:
  - build
  - test

build-job:
  tags:
    - docker
  stage: build
  script:
    - echo "Hello world!"
    - npm install # install all the npm modules

test-job-example:
  tags:
    - docker
  stage: test
  script:
    # - npm install unit.js
    # - npm install -g mocha
    # - npm install jsdom
    # - npm install jquery
    - npx mocha test
