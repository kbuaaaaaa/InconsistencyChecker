image: node

stages:
 - lint
 - build
 - test

eslint:
  tags:
    - docker
  stage: lint
  script:
    - npm install eslint
    - npm init @eslint/config
    - ./node_modules/eslint/bin/eslint.js .

build-job:
  tags:
    - docker
  stage: build
  script:
    - echo "Hello world!"

test-job-1:
  tags:
    - docker
  stage: test
  script:
    - echo "This job tests something"
