image: node

stages:
  - build
  - test

build-job:
  tags:
    - docker
  stage: build
  script:
    - echo "Hello world!"
    - npm install
    # - npx eslint ./project/extension/js

test-job-example:
  tags:
    - docker
  stage: test
  script:
    - npm install
    - npx mocha test
